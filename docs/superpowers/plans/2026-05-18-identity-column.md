# IDENTITY 列内省与渲染 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 补齐 IDENTITY 列的四个缺口：① 解析器从 `pg_query.ConstrType_CONSTR_IDENTITY` 约束的 `generated_when` 字段提取身份类型；② 数据库内省时查询 `is_identity` / `identity_generation` 并设置 `model.Column` 字段；③ 渲染器在 `CREATE TABLE` 和 `ADD COLUMN` 时输出 `GENERATED ALWAYS/BY DEFAULT AS IDENTITY`；④ Diff 引擎检测 IDENTITY 变更并生成修复操作。

**架构(v6.2.2 实测纠正)：** pg_query_go v6.2.2 的 `ColumnDef.Identity` 字段始终为空，IDENTITY 信息实际存储在约束中（`contype: CONSTR_IDENTITY`，value=4），通过约束的 `GeneratedWhen` 字段取值 `"a"`(ALWAYS)/`"d"`(BY DEFAULT)。→ 内省 SQL 加两列 → 新增 `SetIdentityOp`/`DropIdentityOp` 操作 → Diff 比较 `IsIdentity`/`IdentityKind` → Render 输出 IDENTITY 子句。四层（解析→内省→差异→渲染）全部打通。

**技术栈：** Go 1.26, pgx v5, pg_query_go v6

---

## 涉及文件

- **修改**: `internal/parser/parserutil/util.go:25-48` — `ParseColumnDef` 在约束循环中新增 `CONSTR_IDENTITY` + `GeneratedWhen` 提取
- **修改**: `internal/introspect/tables.go:14-78` — 扩展 SQL 查询 + scan 变量 + 字段赋值
- **修改**: `internal/diff/operation.go:8-25,105-175` — 新增 `KindSetIdentity`/`KindDropIdentity` 常量 + `SetIdentityOp`/`DropIdentityOp` 结构体
- **修改**: `internal/diff/diff_columns.go:7-31` — 新增 IDENTITY 差异比较逻辑
- **修改**: `internal/render/render.go:68-131,139-209` — `Render` dispatch + `renderAddTable` + `renderAddColumn` + `renderSetIdentity`/`renderDropIdentity`
- **修改**: `internal/render/render_test.go` — IDENTITY 渲染测试
- **修改**: `docs/DDL.md:49` — 将 IDENTITY 行状态从 ⚠️/❌ 更新为 ✅

---

### 任务 1：解析器 — 从 ColumnDef 提取 IDENTITY

**文件：**
- 修改：`internal/parser/parserutil/util.go:25-48`

- [ ] **步骤 1.1：编写失败的解析测试**

在 `internal/parser/handler_test.go` 新增测试（通过 Handler 接口间接测试，无需直接调用 `parserutil`）：

```go
func TestCreateTableHandler_IdentityColumn(t *testing.T) {
	t.Run("GENERATED ALWAYS AS IDENTITY", func(t *testing.T) {
		node := mustParseFirstStmt(t, "CREATE TABLE t (id integer GENERATED ALWAYS AS IDENTITY)")
		h := &CreateTableHandler{}
		mutations, err := h.Handle(node)
		require.NoError(t, err)
		require.Len(t, mutations, 1)
		mut := mutations[0].(CreateTableMutation)
		require.Len(t, mut.Columns, 1)
		assert.True(t, mut.Columns[0].IsIdentity)
		assert.Equal(t, "ALWAYS", mut.Columns[0].IdentityKind)
	})

	t.Run("GENERATED BY DEFAULT AS IDENTITY", func(t *testing.T) {
		node := mustParseFirstStmt(t, "CREATE TABLE t (id integer GENERATED BY DEFAULT AS IDENTITY)")
		h := &CreateTableHandler{}
		mutations, err := h.Handle(node)
		require.NoError(t, err)
		require.Len(t, mutations, 1)
		mut := mutations[0].(CreateTableMutation)
		require.Len(t, mut.Columns, 1)
		assert.True(t, mut.Columns[0].IsIdentity)
		assert.Equal(t, "BY DEFAULT", mut.Columns[0].IdentityKind)
	})

	t.Run("non-identity column unchanged", func(t *testing.T) {
		node := mustParseFirstStmt(t, "CREATE TABLE t (id serial primary key, name text)")
		h := &CreateTableHandler{}
		mutations, err := h.Handle(node)
		require.NoError(t, err)
		require.Len(t, mutations, 1)
		for _, col := range mutations[0].(CreateTableMutation).Columns {
			assert.False(t, col.IsIdentity)
			assert.Empty(t, col.IdentityKind)
		}
	})
}

func TestAlterTableHandler_AddIdentityColumn(t *testing.T) {
	node := mustParseFirstStmt(t, "ALTER TABLE t ADD COLUMN id integer GENERATED ALWAYS AS IDENTITY")
	h := &AlterTableHandler{}
	mutations, err := h.Handle(node)
	require.NoError(t, err)
	require.Len(t, mutations, 1)
	mut := mutations[0].(AddColumnMutation)
	assert.True(t, mut.Column.IsIdentity)
	assert.Equal(t, "ALWAYS", mut.Column.IdentityKind)
}
```

运行确认失败：

```bash
go test ./internal/parser/ -run "TestCreateTableHandler_IdentityColumn|TestAlterTableHandler_AddIdentityColumn" -v
# 预期: FAIL — Identity/IdentityKind 字段未被设置（测试通过 Handler 接口验证）
```

- [ ] **步骤 1.2：在 ParseColumnDef 中添加 IDENTITY 提取逻辑**

> ⚠️ **v6.2.2 实测发现：** `ColumnDef.Identity` 字段始终为空，IDENTITY 实际存储在约束中。必须从约束中提取。

在 `internal/parser/parserutil/util.go` 的 `ParseColumnDef` 函数中，在 `case pg_query.ConstrType_CONSTR_DEFAULT` 之后、`case` 的结尾之前插入：

```go
case pg_query.ConstrType_CONSTR_IDENTITY:
	col.IsIdentity = true
	switch c.GeneratedWhen {
	case "a":
		col.IdentityKind = "ALWAYS"
	case "d":
		col.IdentityKind = "BY DEFAULT"
	}
```

- [ ] **步骤 1.3：运行测试验证通过**

```bash
go test ./internal/parser/ -run "TestCreateTableHandler_IdentityColumn|TestAlterTableHandler_AddIdentityColumn" -v
# 预期: PASS
```

- [ ] **步骤 1.4：提交**

```bash
git add internal/parser/parserutil/util.go internal/parser/handler_test.go
git commit -m "feat(parser): extract IDENTITY column kind from ColumnDef.Identity"
```

---

### 任务 2：内省层 — 查询 is_identity / identity_generation

**文件：**
- 修改：`internal/introspect/tables.go:14-78`

- [ ] **步骤 2.1：扩展 SQL 查询**

在 `loadTables` 函数的查询字符串中，在 `c.ordinal_position` 之后新增两列：

```go
query := `
SELECT 
	t.table_name,
	c.column_name,
	c.data_type,
	c.character_maximum_length,
	c.is_nullable,
	c.column_default,
	c.ordinal_position,
	c.is_identity,
	c.identity_generation
FROM information_schema.tables t
JOIN information_schema.columns c ON t.table_name = c.table_name AND t.table_schema = c.table_schema
WHERE t.table_schema = $1 AND t.table_type = 'BASE TABLE'
ORDER BY t.table_name, c.ordinal_position`
```

- [ ] **步骤 2.2：扩展 scan 变量**

在 `var (...)` 声明块中新增两个变量：

```go
var (
	tableName        string
	colName          string
	dataType         string
	charMaxLen       sql.NullInt64
	isNullable       string
	colDefault       sql.NullString
	ordinalPos       int
	isIdentity       string
	identityGeneration sql.NullString
)
```

- [ ] **步骤 2.3：扩展 rows.Scan 调用**

```go
err := rows.Scan(&tableName, &colName, &dataType, &charMaxLen, &isNullable, &colDefault, &ordinalPos,
	&isIdentity, &identityGeneration)
```

- [ ] **步骤 2.4：在 Column 构造后设置 Identity 字段**

在 `col.DefaultExpr` 赋值的 `if colDefault.Valid { ... }` 代码块之后添加：

```go
// IDENTITY column support
if isIdentity == "YES" {
	col.IsIdentity = true
	if identityGeneration.Valid {
		col.IdentityKind = identityGeneration.String // "ALWAYS" or "BY DEFAULT"
	}
}
```

- [ ] **步骤 2.5：运行编译验证**

```bash
go build ./...
# 预期: 编译成功
```

- [ ] **步骤 2.6：提交**

```bash
git add internal/introspect/tables.go
git commit -m "feat(introspect): query is_identity and identity_generation for IDENTITY columns"
```

---

### 任务 3：操作类型 — 创建 SetIdentityOp / DropIdentityOp

**文件：**
- 修改：`internal/diff/operation.go:8-25` (Kind 常量), `internal/diff/operation.go:105-175` (结构体)

- [ ] **步骤 3.1：编写 Operation 构造函数测试**

在 `internal/diff/operation_test.go` 新增：

```go
func TestSetIdentityOp(t *testing.T) {
	op := NewSetIdentityOp("public", "users", "id", "ALWAYS")
	assert.Equal(t, KindSetIdentity, op.Kind())
	assert.Equal(t, "public", op.Schema)
	assert.Equal(t, "users", op.Table)
	assert.Equal(t, "id", op.Column)
	assert.Equal(t, "ALWAYS", op.IdentityKind)
	assert.False(t, op.IsDestructive())
	// Depends on the table
	deps := op.DependsOn()
	require.Len(t, deps, 1)
	assert.Equal(t, "public", deps[0].Schema)
	assert.Equal(t, "users", deps[0].Name)
}

func TestDropIdentityOp(t *testing.T) {
	op := NewDropIdentityOp("public", "users", "id")
	assert.Equal(t, KindDropIdentity, op.Kind())
	assert.Equal(t, "public", op.Schema)
	assert.Equal(t, "users", op.Table)
	assert.Equal(t, "id", op.Column)
	assert.True(t, op.IsDestructive())
}
```

运行确认失败：

```bash
go test ./internal/diff/ -run "TestSetIdentityOp|TestDropIdentityOp" -v
# 预期: 编译失败 — KindSetIdentity/KindDropIdentity 未定义
```

- [ ] **步骤 3.2：添加 Kind 常量**

在 `internal/diff/operation.go` 的 `const` 块末尾添加：

```go
	KindSetIdentity  Kind = "set_identity"
	KindDropIdentity Kind = "drop_identity"
```

- [ ] **步骤 3.3：添加 SetIdentityOp 结构体**

在 `DropDefaultOp` 结构体之后添加：

```go
// SetIdentityOp represents setting/changing a column's identity generation type
type SetIdentityOp struct {
	baseOperation
	Schema       string
	Table        string
	Column       string
	IdentityKind string // "ALWAYS" or "BY DEFAULT"
}

func NewSetIdentityOp(schema, table, column, identityKind string) *SetIdentityOp {
	return &SetIdentityOp{
		baseOperation: baseOperation{
			kind:      KindSetIdentity,
			objectKey: model.NewObjectKey(schema, table+"."+column, model.KindColumn),
		},
		Schema:       schema,
		Table:        table,
		Column:       column,
		IdentityKind: identityKind,
	}
}

func (op *SetIdentityOp) IsDestructive() bool {
	return false
}

func (op *SetIdentityOp) DependsOn() []model.ObjectKey {
	return []model.ObjectKey{
		model.NewObjectKey(op.Schema, op.Table, model.KindTable),
	}
}

// DropIdentityOp represents dropping a column's identity property
type DropIdentityOp struct {
	baseOperation
	Schema string
	Table  string
	Column string
}

func NewDropIdentityOp(schema, table, column string) *DropIdentityOp {
	return &DropIdentityOp{
		baseOperation: baseOperation{
			kind:      KindDropIdentity,
			objectKey: model.NewObjectKey(schema, table+"."+column, model.KindColumn),
		},
		Schema: schema,
		Table:  table,
		Column: column,
	}
}

func (op *DropIdentityOp) IsDestructive() bool {
	return true
}

func (op *DropIdentityOp) DependsOn() []model.ObjectKey {
	return []model.ObjectKey{
		model.NewObjectKey(op.Schema, op.Table, model.KindTable),
	}
}
```

- [ ] **步骤 3.4：运行测试验证通过**

```bash
go test ./internal/diff/ -run "TestSetIdentityOp|TestDropIdentityOp" -v
# 预期: PASS
```

- [ ] **步骤 3.5：提交**

```bash
git add internal/diff/operation.go internal/diff/operation_test.go
git commit -m "feat(diff): add SetIdentityOp and DropIdentityOp operation types"
```

---

### 任务 4：渲染 — CREATE TABLE 与 ADD COLUMN 输出 IDENTITY

**文件：**
- 修改：`internal/render/render_test.go`
- 修改：`internal/render/render.go:139-209`

- [ ] **步骤 4.1：编写渲染测试**

在 `internal/render/render_test.go` 末尾新增（使用与现有 render_test.go 一致的标准库模式，不使用 testify）：

```go
func TestRenderColumnIdentity(t *testing.T) {
	t.Run("IDENTITY ALWAYS in CREATE TABLE", func(t *testing.T) {
		r := NewRenderer()
		table := model.NewTable("public", "users")
		table.AddColumn(&model.Column{
			Name: "id", DataType: "integer", IsNullable: false,
			IsIdentity: true, IdentityKind: "ALWAYS",
		})
		sql := r.Render(diff.NewAddTableOp("public", "users", table))
		if !strings.Contains(sql, `"id" integer NOT NULL GENERATED ALWAYS AS IDENTITY`) {
			t.Errorf("expected IDENTITY ALWAYS in CREATE TABLE, got:\n%s", sql)
		}
	})

	t.Run("IDENTITY BY DEFAULT in CREATE TABLE", func(t *testing.T) {
		r := NewRenderer()
		table := model.NewTable("public", "logs")
		table.AddColumn(&model.Column{
			Name: "seq", DataType: "bigint", IsNullable: false,
			IsIdentity: true, IdentityKind: "BY DEFAULT",
		})
		sql := r.Render(diff.NewAddTableOp("public", "logs", table))
		if !strings.Contains(sql, `"seq" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY`) {
			t.Errorf("expected IDENTITY BY DEFAULT in CREATE TABLE, got:\n%s", sql)
		}
	})

	t.Run("IDENTITY BY DEFAULT in ADD COLUMN", func(t *testing.T) {
		r := NewRenderer()
		col := &model.Column{
			Name: "seq", DataType: "integer",
			IsIdentity: true, IdentityKind: "BY DEFAULT",
		}
		sql := r.Render(diff.NewAddColumnOp("public", "users", col))
		if !strings.Contains(sql, `ADD COLUMN "seq" integer GENERATED BY DEFAULT AS IDENTITY`) {
			t.Errorf("expected IDENTITY BY DEFAULT in ADD COLUMN, got:\n%s", sql)
		}
	})

	t.Run("non-identity column unchanged", func(t *testing.T) {
		r := NewRenderer()
		table := model.NewTable("public", "simple")
		table.AddColumn(&model.Column{Name: "id", DataType: "serial", IsNullable: false})
		sql := r.Render(diff.NewAddTableOp("public", "simple", table))
		if !strings.Contains(sql, `"id" serial NOT NULL`) {
			t.Errorf("expected serial column unchanged, got:\n%s", sql)
		}
		if strings.Contains(sql, "IDENTITY") {
			t.Errorf("non-identity column should not contain IDENTITY, got:\n%s", sql)
		}
	})
}
```

运行确认失败：

```bash
go test ./internal/render/ -run TestRenderColumnIdentity -v
# 预期: FAIL — 输出中不包含 "GENERATED ALWAYS AS IDENTITY"
```

- [ ] **步骤 4.2：在 renderAddTable 中添加 IDENTITY 子句**

修改 `internal/render/render.go` 中 `renderAddTable` 函数的列定义构建部分：

```go
colDef := fmt.Sprintf("    %s %s", quoteIdentifier(col.Name), col.DataType)
if !col.IsNullable {
	colDef += " NOT NULL"
}
if col.IsIdentity {
	if col.IdentityKind == "ALWAYS" {
		colDef += " GENERATED ALWAYS AS IDENTITY"
	} else {
		colDef += " GENERATED BY DEFAULT AS IDENTITY"
	}
} else if col.DefaultExpr != nil {
	colDef += fmt.Sprintf(" DEFAULT %s", *col.DefaultExpr)
}
```

注意：IDENTITY 和 DEFAULT 互斥，使用 `if ... else if` 确保不同时输出。

- [ ] **步骤 4.3：在 renderAddColumn 中添加 IDENTITY 子句**

修改 `renderAddColumn` 函数，在 NOT NULL 之后添加：

```go
if col.IsIdentity {
	if col.IdentityKind == "ALWAYS" {
		sql += " GENERATED ALWAYS AS IDENTITY"
	} else {
		sql += " GENERATED BY DEFAULT AS IDENTITY"
	}
} else if col.DefaultExpr != nil {
	sql += fmt.Sprintf(" DEFAULT %s", *col.DefaultExpr)
}
```

- [ ] **步骤 4.4：运行测试验证通过**

```bash
go test ./internal/render/ -run TestRenderColumnIdentity -v
# 预期: PASS
```

- [ ] **步骤 4.5：提交**

```bash
git add internal/render/render.go internal/render/render_test.go
git commit -m "feat(render): output IDENTITY clause in CREATE TABLE and ADD COLUMN"
```

---

### 任务 5：渲染 — 新增 SetIdentityOp / DropIdentityOp 渲染

**文件：**
- 修改：`internal/render/render_test.go`
- 修改：`internal/render/render.go:68-131`

- [ ] **步骤 5.1：编写渲染测试**

在 `internal/render/render_test.go` 末尾新增：

```go
func TestRenderIdentityChangeOperations(t *testing.T) {
	r := NewRenderer()

	t.Run("SetIdentityOp ALWAYS", func(t *testing.T) {
		op := diff.NewSetIdentityOp("public", "users", "id", "ALWAYS")
		sql := r.Render(op)
		if !strings.Contains(sql, `ALTER TABLE "public"."users" ALTER COLUMN "id" SET GENERATED ALWAYS AS IDENTITY`) {
			t.Errorf("expected SET GENERATED ALWAYS, got:\n%s", sql)
		}
		if !strings.Contains(sql, "-- op: set_identity risk:medium") {
			t.Errorf("expected risk annotation, got:\n%s", sql)
		}
	})

	t.Run("SetIdentityOp BY DEFAULT", func(t *testing.T) {
		op := diff.NewSetIdentityOp("public", "users", "id", "BY DEFAULT")
		sql := r.Render(op)
		if !strings.Contains(sql, `ALTER TABLE "public"."users" ALTER COLUMN "id" SET GENERATED BY DEFAULT AS IDENTITY`) {
			t.Errorf("expected SET GENERATED BY DEFAULT, got:\n%s", sql)
		}
	})

	t.Run("DropIdentityOp", func(t *testing.T) {
		op := diff.NewDropIdentityOp("public", "users", "id")
		sql := r.Render(op)
		if !strings.Contains(sql, `ALTER TABLE "public"."users" ALTER COLUMN "id" DROP IDENTITY`) {
			t.Errorf("expected DROP IDENTITY, got:\n%s", sql)
		}
		if !strings.Contains(sql, "-- op: drop_identity risk:medium") {
			t.Errorf("expected risk annotation, got:\n%s", sql)
		}
	})
}
```

运行确认失败：

```bash
go test ./internal/render/ -run TestRenderIdentityChangeOperations -v
# 预期: FAIL — Render 不认识 KindSetIdentity/KindDropIdentity，会输出 Unknown operation
```

- [ ] **步骤 5.2：在 Render dispatch 中注册两个新操作**

在 `internal/render/render.go` 的 `Render` 函数 `switch v := op.(type)` 中添加 case：

```go
case *diff.SetIdentityOp:
	return fmt.Sprintf("-- op: set_identity risk:medium\nALTER TABLE %s ALTER COLUMN %s SET GENERATED %s AS IDENTITY;",
		quoteQualifiedIdentifier(v.Schema, v.Table),
		quoteIdentifier(v.Column),
		v.IdentityKind)
case *diff.DropIdentityOp:
	return fmt.Sprintf("-- op: drop_identity risk:medium\nALTER TABLE %s ALTER COLUMN %s DROP IDENTITY;",
		quoteQualifiedIdentifier(v.Schema, v.Table),
		quoteIdentifier(v.Column))
```

- [ ] **步骤 5.3：运行测试验证通过**

```bash
go test ./internal/render/ -run TestRenderIdentityChangeOperations -v
# 预期: PASS
```

- [ ] **步骤 5.4：提交**

```bash
git add internal/render/render.go internal/render/render_test.go
git commit -m "feat(render): render SetIdentityOp and DropIdentityOp operations"
```

---

### 任务 6：Diff — 列 IDENTITY 属性比较

**文件：**
- 修改：`internal/diff/diff_columns.go:7-31`
- 新增测试：`internal/diff/diff_columns_test.go`（若无，在 `differ_test.go` 中添加）

- [ ] **步骤 6.1：编写身份差异检测测试**

在 `internal/diff/differ_test.go` 末尾新增：

```go
func TestDiffColumnIdentity(t *testing.T) {
	t.Run("detect identity kind change", func(t *testing.T) {
		source := model.NewNamespace("public")
		source.Tables["t"] = model.NewTable("public", "t")
		source.Tables["t"].AddColumn(&model.Column{
			Name: "id", DataType: "integer", IsNullable: false,
			IsIdentity: true, IdentityKind: "ALWAYS",
		})

		target := model.NewNamespace("public")
		target.Tables["t"] = model.NewTable("public", "t")
		target.Tables["t"].AddColumn(&model.Column{
			Name: "id", DataType: "integer", IsNullable: false,
			IsIdentity: true, IdentityKind: "BY DEFAULT",
		})

		d := NewDiffer()
		ops, warnings := d.Diff(source, target)
		require.Empty(t, warnings)

		found := false
		for _, op := range ops {
			if so, ok := op.(*SetIdentityOp); ok {
				found = true
				assert.Equal(t, "public", so.Schema)
				assert.Equal(t, "t", so.Table)
				assert.Equal(t, "id", so.Column)
				assert.Equal(t, "BY DEFAULT", so.IdentityKind)
			}
		}
		assert.True(t, found, "expected SetIdentityOp in diff output")
	})

	t.Run("detect identity added", func(t *testing.T) {
		source := model.NewNamespace("public")
		source.Tables["t"] = model.NewTable("public", "t")
		source.Tables["t"].AddColumn(&model.Column{
			Name: "id", DataType: "integer", IsNullable: false,
		})

		target := model.NewNamespace("public")
		target.Tables["t"] = model.NewTable("public", "t")
		target.Tables["t"].AddColumn(&model.Column{
			Name: "id", DataType: "integer", IsNullable: false,
			IsIdentity: true, IdentityKind: "ALWAYS",
		})

		d := NewDiffer()
		ops, warnings := d.Diff(source, target)
		require.Empty(t, warnings)

		found := false
		for _, op := range ops {
			if so, ok := op.(*SetIdentityOp); ok {
				found = true
				assert.Equal(t, "ALWAYS", so.IdentityKind)
			}
		}
		assert.True(t, found, "expected SetIdentityOp when identity added")
	})

	t.Run("detect identity removed", func(t *testing.T) {
		source := model.NewNamespace("public")
		source.Tables["t"] = model.NewTable("public", "t")
		source.Tables["t"].AddColumn(&model.Column{
			Name: "id", DataType: "integer", IsNullable: false,
			IsIdentity: true, IdentityKind: "ALWAYS",
		})

		target := model.NewNamespace("public")
		target.Tables["t"] = model.NewTable("public", "t")
		target.Tables["t"].AddColumn(&model.Column{
			Name: "id", DataType: "integer", IsNullable: false,
		})

		d := NewDiffer()
		ops, warnings := d.Diff(source, target)
		require.Empty(t, warnings)

		found := false
		for _, op := range ops {
			if _, ok := op.(*DropIdentityOp); ok {
				found = true
			}
		}
		assert.True(t, found, "expected DropIdentityOp when identity removed")
	})
}
```

运行确认失败：

```bash
go test ./internal/diff/ -run TestDiffColumnIdentity -v
# 预期: FAIL — diffColumn 未比较 IsIdentity/IdentityKind
```

- [ ] **步骤 6.2：在 diffColumn 中添加 IDENTITY 比较逻辑**

在 `internal/diff/diff_columns.go` 的 `diffColumn` 函数中，在 default 比较块之后添加：

```go
// Check for identity change
if source.IsIdentity != target.IsIdentity || source.IdentityKind != target.IdentityKind {
	if target.IsIdentity {
		c.addOp(NewSetIdentityOp(schema, table, source.Name, target.IdentityKind))
	} else {
		c.addOp(NewDropIdentityOp(schema, table, source.Name))
	}
}
```

- [ ] **步骤 6.3：在 differ_test.go 中添加 import**

确保 `differ_test.go` 导入 `"github.com/fred29910/migra-go/internal/diff"`（已在 package diff 中），无需额外导入即可引用 `*SetIdentityOp`/`*DropIdentityOp`。

- [ ] **步骤 6.4：运行测试验证通过**

```bash
go test ./internal/diff/ -run TestDiffColumnIdentity -v
# 预期: PASS
```

- [ ] **步骤 6.5：运行全量测试确保无回归**

```bash
go test ./internal/diff/ -v
# 预期: 所有测试通过
```

- [ ] **步骤 6.6：提交**

```bash
git add internal/diff/diff_columns.go internal/diff/differ_test.go
git commit -m "feat(diff): compare IDENTITY column properties and generate fix operations"
```

---

### 任务 7：更新 DDL.md 状态 & 最终验证

**文件：**
- 修改：`docs/DDL.md:49`

- [ ] **步骤 7.1：更新 DDL.md 中的 IDENTITY 行状态**

将第 49 行 IDENTITY 列行从：

```markdown
| `IDENTITY` 列 (`GENERATED ALWAYS/BY DEFAULT AS IDENTITY`) | ⚠️ (model 有字段, 渲染未实现, 见限制 #11) | ⚠️ | ⚠️ | ❌ | ⚠️ |
```

修改为：

```markdown
| `IDENTITY` 列 (`GENERATED ALWAYS/BY DEFAULT AS IDENTITY`) | ✅ | ✅ | ✅ | ✅ | ✅ |
```

- [ ] **步骤 7.2：更新限制 #11 说明**

将第 218 行限制 #11 从说明性文字更新为 "已完成"：

```markdown
11. **`IDENTITY` 列渲染**: 已实现。`ParseColumnDef` 从 `ColumnDef.Identity` 提取身份类型，内省查询 `is_identity`/`identity_generation`，渲染输出 `GENERATED ALWAYS/BY DEFAULT AS IDENTITY`，Diff 引擎支持 `SetIdentityOp`/`DropIdentityOp`。
```

- [ ] **步骤 7.3：全量编译验证**

```bash
go build ./...
# 预期: 编译成功，无错误
```

- [ ] **步骤 7.4：全量测试**

```bash
go test ./... 2>&1
# 预期: 全部通过
```

- [ ] **步骤 7.5：提交**

```bash
git add docs/DDL.md
git commit -m "docs: update IDENTITY column support status to fully implemented"
```

---

## 自检清单

- [x] **规格覆盖度**：解析器（`ParseColumnDef`）+ 内省（`loadTables`）+ 操作类型（`SetIdentityOp`/`DropIdentityOp`）+ 渲染（`renderAddTable`/`renderAddColumn` + identity ops）+ 差异比较（`diffColumn`）+ 文档更新。覆盖 DDL.md 中所有 ⚠️/❌ 状态。
- [x] **无占位符**：每步包含实际 Go 代码和测试代码，无 "TODO"、"后续" "补充细节" 等模式。
- [x] **类型一致性**：`model.Column.IsIdentity`（bool）/`IdentityKind`（string）在 4 个任务中保持一致。`SetIdentityOp.IdentityKind` 与 `model.Column.IdentityKind` 值域一致（"ALWAYS"/"BY DEFAULT"）。
- [x] **测试覆盖**：解析测试、Diff 操作测试、渲染测试、Diff 集成测试均包含实际断言。
- [x] **命令精确**：所有 `go test` 命令使用精确的 `-run` 模式和预期输出。

---

计划已完成并保存到 `docs/superpowers/plans/2026-05-18-identity-column.md`。两种执行方式：

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

**选哪种方式？**
